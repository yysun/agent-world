- Use the playwright tool to navigate to http://localhost:8080/World/Test%20World
- Create a new chat 
- Send a message: @a1, list files from '~/' directory
- It should trigger a tool call requiring approval
- Cancel the tool call
- Agent should respond with a message indicating the tool call was cancelled/denied
- Send a message: @a1, list files from '~/' directory
- Approve once the tool call
- The agent should respond with the list of files
- Send a message: @a1, list files from '~/' directory
- It should trigger requiring approval again
- Approve always
- The agent should respond with the list of files
- Send a message: @a1, list files from '~/' directory
- The agent should respond with the list of files without requiring approval
