/**
 * Agents Feature Public Exports
 *
 * Purpose:
 * - Provide the public agents-feature entry point for renderer imports.
 *
 * Key Features:
 * - Re-exports agent-owned workspace editor UI.
 *
 * Implementation Notes:
 * - Shared generic shells still come from `design-system/`; this surface is agent-specific.
 *
 * Recent Changes:
 * - 2026-04-11: Added the initial agents feature export surface.
 */

export * from './components';