{
  "name": "@agent-world/web",
  "version": "1.0.0",
  "description": "Web frontend for Agent World - built with AppRun and Vite",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "tsc --noEmit",
    "dev": "vite dev --port 8080 --open",
    "build": "vite build --outDir ../dist/public && vite build --outDir ../public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "apprun": "^3.38.0"
  },
  "dependencies": {
    "@types/dompurify": "^3.0.5",
    "@types/marked": "^5.0.2",
    "dompurify": "^3.2.6",
    "doodle.css": "^0.0.2",
    "marked": "^16.1.1"
  }
}